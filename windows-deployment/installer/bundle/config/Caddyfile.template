# Caddy reverse proxy configuration for SolarCRM HTTPS
# Generated by Enable-HTTPS.ps1
# Caddy documentation: https://caddyserver.com/docs/

# ── For a real domain (e.g. DuckDNS or custom domain) ────────────────────────
#
# your-domain.duckdns.org {
#     reverse_proxy localhost:3000
#     encode gzip
#     header {
#         Strict-Transport-Security "max-age=31536000; includeSubDomains"
#         X-Frame-Options "SAMEORIGIN"
#         X-Content-Type-Options "nosniff"
#     }
# }
#
# ── For LAN-only with self-signed cert ───────────────────────────────────────
#
# :443 {
#     tls internal
#     reverse_proxy localhost:3000
#     encode gzip
# }
# :80 {
#     redir https://{host}{uri} permanent
# }
#
# Run Enable-HTTPS.ps1 to generate the actual Caddyfile automatically.
